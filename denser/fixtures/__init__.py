"""Built-in fixtures (golden evaluation tasks) shipped with denser.

Fixtures live in `denser/fixtures/golden/<task_type>/*.json`. Each JSON file
encodes one `GoldenTask`. See `denser.eval` for loading and running them.
"""
